# run npm run scss:watch first.
mkdir src/static/compiled
mv src/static/compiled/* src/lib/styles/compiled
mv src/static src/static/compiled/app.css 
rm -rf src/lib/styles/compiled/compiled
rm -rf src/lib/styles/compiled